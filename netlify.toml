[build]
  command = "echo \"const SUPABASE_URL = '$SUPABASE_URL'; const SUPABASE_ANON_KEY = '$SUPABASE_ANON_KEY'; const db = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\" > js/supabase-config.js"
  publish = "."
